version: '3'

services:
  scanner:
    build: 
      context: .
      dockerfile: Dockerfile
    image: codebanesr/scanner:latest
    ports:
      - "8000:8000"
    volumes:
      - shared_data:/app/shared_data

volumes:
  shared_data: